# Public env vars (safe to expose to browser)
NEXT_PUBLIC_API_URL=http://localhost:5000/api

# Used ONLY in Next.js middleware (server-side Edge Runtime)
# Must match the JWT_SECRET in your backend .env
JWT_SECRET=your_super_secret_jwt_key_here_must_be_at_least_32_characters_long
